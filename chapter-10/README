Begin


#####chapter10####generic
1、struct<T>
2、enum<T>
3、method: impl<T>
4、function: fn name<T>



#########trait
A trait defines functionality a particular type has and can share with other types. We can user traits to define shared behavior in an abstract way. We can user trait bounds to specify that a generic type can be any type that has certain behavior.
